name: Core Foundation V2 Migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Core foundation migration completed; workflow retained temporarily for audit history."
